# Day 0 台本 — キックオフ（8/17 月）

- 使える時間は約60分です（15:05授業終了→15:10ごろ開始→16:10ごろ解散。時間の使い方の全体像は docs/afterschool-plan.md）。
- ゴール：役割に最終合意し、チームの決めごとを共有し、機材を配布し、Discordに集まります。残った時間で、**学校PCへの開発環境の導入**を始めます（軽いものから。続きは8/18(火)にやります。学校PCは1人1アカウントなので、GitHubの認証も各自ふつうに行って大丈夫です）。
- Git一周演習——全員が「ブランチを作る→コミットをする→pushする→PRを作る→レビューを受ける→マージされる」という流れを1回自分の手でやること——は、**学校PCの導入が済んだ8/18(火)に実施します**（手順は3章）。Day 0当日は、企画者が画面で流れを見せるだけにします。
  - ブランチ：本体（main）に影響を与えずに作業するための、枝分かれした作業用コピーです
  - コミット：変更内容の記録です（ゲームのセーブポイントのようなものです）
  - push：自分のPCでの記録をGitHub側に送って反映することです
  - PR（Pull Request）：「この変更を本体に取り込んでください」という提案です。チームの誰かが確認してから取り込みます
  - マージ：提案された変更を本体に取り込むことです
- 進行役：企画者が務めます。

## 0. 事前準備（企画者側・8/17より前）

8/18(火)のGit演習までに、メンバーがpushできる状態にしておくための前提です。

- [x] GitHub（書いたプログラムの変更履歴を記録・共有できるWebサービス）上にリモートリポジトリ（プロジェクト一式の保管場所。フォルダごと、変更履歴つきで置いておく場所）`classroom-temp-map`（public）を作成する。→ 2026-08-08作成済み: https://github.com/rei-okabayashi/classroom-temp-map
- [ ] メンバー全員をCollaborator（リポジトリに変更を送る権限を持つ共同作業メンバー）として招待する。招待のAccept（承諾）状況はDay 0当日に全員で確認する。
- [ ] 各自のローカルで `git clone`（GitHub上のリポジトリを自分のPCへ丸ごと複製すること）をしてもらう（または事前宿題として行ってもらう）。
- [ ] Discordサーバーを作成し、招待リンクを用意する。
- [ ] 機材一式を当日持参する：ノード用ESP32（無線通信ができる小型マイコン。マイコン＝プログラムで動く小さなコンピューター）×4・SHT31（温度と湿度を測るセンサー部品の型番）×4・M5Stack（画面つきの小型マイコン。教壇に置いて受信データを表示・記録します）・USBケーブル・ACアダプタ。ノードは教室に置く測定係の小さなコンピューターです（ESP32＋温湿度センサーのセット。n1〜n4の4台）。
- [ ] 事前アンケート（役割希望）の回答を確認し、希望がかぶった場合は8/16(日)にLINEの1:1で調整を済ませておく（回答締切が8/15(土)23:59のため）。
- [ ] 学校PCでの導入の下見を済ませておく：公式サイトから各インストーラをダウンロードできるか（サイトがブロックされていないか）・インストールが管理者権限で止まらないか・特にUSBドライバが入るか（私有USBメモリと個人PCは学校ルールで持ち込み禁止のため、**配布はせず各自が学校回線で公式サイトからダウンロードする**方式です）。

リモートリポジトリは2026-08-08に作成・公開済みです。残りの事前準備（Collaborator招待・Discordサーバー作成）は8/16に実施します。

## 1. 事前宿題（8/17より前に、各自自宅で）

全員、Day 0当日までに以下を終わらせておきます。

- [ ] GitHubアカウントを作成する（`docs/setup-windows.md` の0章）
- [ ] Discordアカウントを作成する（`docs/setup-discord.md`）
- [ ] 開発環境をセットアップする（`docs/setup-windows.md` の6章まで）
- [ ] 役割希望アンケートに回答する（LINEで届くフォーム・8/15(土)23:59まで）

（`docs/setup-windows.md` は作成済みです。事前宿題としてそのまま案内できます。）

### 宿題が終わっているかの事前確認方法

- 8/16(日)までに、各自「GitHubのユーザー名」と「Discordのユーザー名」を、ふだん使っている連絡手段で企画者に伝えてください。
- あわせて `docs/setup-windows.md` の完了報告をしてください。完了基準は「6章：node-template のコンパイル（検証。書いたプログラムを機械が実行できる形に変換し、文法ミスがないか確かめること）が通ること」までです。実機がまだ手元に無い期間なので、7章（書き込み・シリアル確認）はDay 0以降の放課後にやれば大丈夫です。書き込みとは、コンパイルしたプログラムをUSBケーブル経由でボードに転送することです。
- 企画者は報告が揃っているかをチェックリストで管理し、未報告者には8/16中にリマインドします。
- 8/17の開始時点で報告が無いメンバーがいる場合は、8/18の導入デーで個別フォロー（時間を多めに使う）に回します。

事前確認の連絡手段は **LINE**です（メンバー全員が導入済みです。Discordアカウント自体がこの宿題で作られるため、宿題の進捗連絡はLINEで行います）。

## 2. 当日タイムテーブル（目安60分）

開始を0分として経過時間で表記します（実際の開始時刻はDay 0当日に企画者が案内します）。

| 経過 | 所要 | 内容 |
|---|---|---|
| 0〜5分 | 5分 | 挨拶と目的 |
| 5〜13分 | 8分 | 役割の確認（事前アンケートの希望を発表・最終合意） |
| 13〜18分 | 5分 | 機材配布（受け取りはREADMEの「ノードと設置場所」表の担当欄に記入して記録） |
| 18〜28分 | 10分 | 進め方の説明（Git演習の流れを企画者の画面で見せる・8/18導入デーの案内） |
| 28〜50分 | 22分 | 学校PCへの導入（軽い分：Git for Windows→VSCode→clone→名前とメールの設定） |
| 50〜60分 | 10分 | Discord確認＋今後の進め方＋片付け |

合計60分です（15:05授業終了→15:10ごろ開始→16:10ごろ解散の想定です）。時間が足りない日の削り順は「5. 時間切れ時の持ち越しルール」を参照してください。**学校PCへの導入は途中で終わっても問題ありません**（続きは8/18にやります）。

### 0〜5分：挨拶と目的

- このチームで作るもの（教室の温度ムラ見える化）を一言で確認します。
- 進め方の基本ルールを短く確認します：
  - AIルール4つ（共通テンプレート／データ契約を守る／説明できないコードはマージしない／AI利用箇所をREADMEに明記）を守ります。データ契約とは、ノードと集約の間の約束事です（詳しくは docs/data-contract.md）
  - Gitはfeatureブランチ（機能追加用に切る作業ブランチ）→PR→企画者レビュー→マージという流れで進めます。mainへの直接pushはしません。
  - 他の人のノード実装・発表スライド本体は代わりに作りません（企画者も同じルールです）。手伝うときはペア作業で行います。
  - 企画者が数日不在の日は、レビュー待ちのPRはそのまま積んでおき、ブロックされた人は別のIssue（タスクカード。やることを1件ずつカードにしたもの）に移ります（不在時の暫定ルールです）。
  - 公開リポジトリなので、学校名や教室が特定される写真・情報は書きません。
  - 週次リズム（月曜キックオフ・毎日Discord2行・金曜デモ。詳細はREADME「チームの決めごと」）で進めます。
  - 詰まったら24時間以内に申告し、30分のペア作業をします（遅れは責めません）。

### 5〜13分：役割の確認

- 前提：候補はノード担当（全員1人1台）／集約表示オーナー（1名）／データ分析／発表とりまとめ／筐体（基本は企画者担当。やってみたい人がいれば正式担当として譲ります）。事前アンケートで各自の第1・第2希望は収集済みです。希望がかぶった場合は8/16のLINE 1:1調整で解消済みです（「0. 事前準備」参照）。
- 当日やること：アンケート結果を発表し、最終合意を取るだけです。役割ごとの内容は `docs/roles.md`（役割ひとことカード）を参照してください。
- 集約表示オーナー（M5Stack担当）は、M5Stackが企画者の個人所有物のため、発表後の譲渡対象外であることを再確認してから最終合意します。

### 13〜18分：機材配布

- 各自に担当ノード（ESP32-DevKitC-32E＋SHT31）とUSBケーブルを渡します。集約表示オーナーにはM5Stack一式を渡します。
- 受け取りの記録は新しく紙を作らず、READMEの「ノードと設置場所」表の担当欄にその場で記入して兼ねます（記入は**GitHubユーザー名**で行います。公開リポジトリなので本名は書きません）。
- 動作確認（書き込み・シリアルモニタ（マイコンが出すメッセージをPCの画面で見るための窓）での確認）はこの日にはやりません。8/17週の放課後に `docs/tasks-initial.md` のM2として順次進めます。

### 18〜28分：進め方の説明

- Git一周演習の流れ（3章）を、企画者が自分の画面を見せながら一巡します（この日はメンバーは操作しません）。
- 明日8/18(火)が「学校PC導入デー」であること、導入が済んだ人からGit演習を完走することを伝えます。

### 28〜50分：学校PCへの導入（軽い分）

- 各自の学校PCアカウントで、docs/setup-windows.md の5章（Git for Windows・VSCode・clone・名前とメールの設定）を進めます。インストーラは各自が公式サイトからダウンロードします（回線が混まないよう、開始のタイミングを少しずつずらします）。
- 学校PCは1人1アカウントなので、GitHubの認証（初回pushのブラウザ確認）も各自ふつうに行って大丈夫です。
- 終わらなくてOKです。残りとArduino IDE系の重い導入は8/18にやります。

### 50〜60分：Discord＋今後の進め方＋片付け

- Discordサーバーへの参加を確認します（宿題でアカウントは作成済みのはずです）。
- チャンネル構成・連絡ルールを簡単に紹介します。
- 明日8/18(火)の導入デーと、8/21(金)の中間デモ①までに各自やることを一言ずつ確認します。

## 3. Git一周演習の手順（実施は8/18(火)。Day 0では企画者が画面で流れを見せるだけです）

目的は、`day0/<自分のハンドル>.md` を新規作成して自己紹介を1行書き、ブランチ作成→commit→push→PR作成→企画者レビュー→マージ、を全員が1周することです。

`<handle>` は各自のGitHubユーザー名など、他の人と被らない短い文字列に置き換えてください。

以下のコマンドを順番に実行します（各行の `#` はコメントで、それぞれ何をしているかの説明です）。

```bash
# 1. リポジトリの最新化（ローカルにclone済みの前提）
git checkout main
git pull

# 2. 自分用のブランチを作る
git checkout -b day0/<handle>

# 3. ファイルを作る（VSCodeで day0/<handle>.md を新規作成し、自己紹介を1行書いて保存）
#    例: 「n1担当の◯◯です。よろしくお願いします。」

# 4. 変更をステージしてコミット
git add day0/<handle>.md
git commit -m "day0: <handle>の自己紹介を追加"

# 5. リモートにpush
git push -u origin day0/<handle>
```

続けてブラウザ側の操作：

1. push後にターミナル（コマンドを文字で打ち込むための画面）へ出るURL（またはGitHub上のリポジトリページ）を開き、「Compare & pull request」からPRを作成します。
2. PRの説明欄（`.github/pull_request_template.md` のテンプレート）に沿って、やったことを一言書きます。
3. 企画者がレビューして、問題なければ「Approve」→マージボタンでマージします。
4. マージされたら演習は完了です。

つまずきやすい点（企画者が机間巡回で見るポイント）：

- `git checkout -b` を忘れてmainのまま作業してしまう → ブランチ名を都度声かけで確認します。
- `git push` 時にリモートが無い／認証エラー → 事前準備（0章）でclone・認証が済んでいるか確認します。
- PRのターゲットブランチが `main` になっているか確認します。

## 4. Day 0完了条件チェックリスト

Day 0（8/17）の完了条件：

- [ ] 役割が最終合意済み（事前アンケート＋8/16調整の結果を当日確認した状態）
- [ ] 機材の配布とREADMEの「ノードと設置場所」表への記入が済んでいる
- [ ] 全員がDiscordサーバーに参加済み
- [ ] **全員がリポジトリ招待（8/16送付）をAccept済み**（メールか https://github.com/notifications から承諾。cloneは承諾前でも成功してしまうので、ここで明示的に確認します。未承諾のまま8/18を迎えると、pushが「Permission denied」で止まります）
- [ ] 学校PCへの軽い導入（Git・VSCode・clone）が進んでいる（途中でも可）

8/18（火）導入デーの完了条件（目標。終わらなかった分は8/19以降の放課後に持ち越し、**8/21の週末ラインまでに全員完了**）：

- [ ] 全員の学校PCで開発環境の導入が完了（Arduino IDE・esp32パッケージ・USBドライバまで。コンパイルが通る）
- [ ] 全員の `day0/<handle>.md` PRがマージ済み（Git一周演習の完走）

## 5. 時間切れ時の持ち越しルール

進行が押して60分で終わらない場合の優先順位です。

1. 最優先（当日中に必ず終えます）：役割の最終合意と機材配布。
2. 最初に削ってよいのは学校PCへの導入です（まるごと8/18に回せます）。
3. 次に削ってよいのは進め方の説明のデモ部分です（8/18の演習の直前に見せ直せます）。
4. Discordの確認が残った人には、当日の夜にテキストで参加手順を案内します。
5. 8/18(火)の導入デーに残れない人がいる場合は、その人だけ8/19以降の放課後に個別フォローします。全体の進行は止めません。
