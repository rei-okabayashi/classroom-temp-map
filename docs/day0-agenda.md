# Day 0 台本 — キックオフ（8/17 月）

- 放課後45〜60分しかない前提。訓練は15:05終了。
- ゴール：全員が「ブランチ作成→コミット→push→PR→レビュー→マージ」を1回自分の手でやる。役割を決める。Discordに集まる。
- 進行役：企画者。

## 0. 事前準備（企画者側・8/17より前）

Day 0当日にメンバーがpushできる状態にしておくための前提。

- [ ] GitHub上にリモートリポジトリ `classroom-temp-map`（public）を作成する。
- [ ] メンバー全員をCollaboratorとして招待する。
- [ ] 各自のローカルに `git clone` させる（または事前宿題側でやらせる）。
- [ ] Discordサーバーを作成し、招待リンクを用意する。

【要確認】リモートリポジトリはこの台本を書いた時点(2026-08-01)でまだ未作成。Day 0までに上記を完了させる必要あり。

## 1. 事前宿題（8/17より前に、各自自宅で）

全員、Day 0当日までに以下を終わらせておく。

- [ ] GitHubアカウントを作成する
- [ ] Discordアカウントを作成する
- [ ] `docs/setup-windows.md` の手順を実施する（Arduino IDE等のセットアップ）

（`docs/setup-windows.md` は作成済み。事前宿題としてそのまま案内できる。）

### 宿題が終わっているかの事前確認方法

- 8/16(日)までに、各自「GitHubのユーザー名」と「Discordのユーザー名」をふだん使っている連絡手段で企画者に伝える。
- あわせて `docs/setup-windows.md` の完了報告をする。完了基準は「6章：node-template のコンパイル（検証）が通ること」まで。実機がまだ手元に無い期間なので、7章（書き込みとシリアル確認）はDay 0以降の放課後にやればよい。
- 企画者は報告が揃っているかをチェックリストで管理し、未報告者には8/16中にリマインドする。
- 8/17の開始時点で報告が無いメンバーがいる場合、そのメンバーのGit一周演習は個別フォロー（放課後の時間を多めに使う）に回す。

事前確認の連絡手段は **LINE**（メンバー全員が導入済み。Discordアカウント自体がこの宿題で作られるため、宿題の進捗連絡はLINEで行う）。

## 2. 当日タイムテーブル（目安60分、最短45分）

開始を0分として経過時間で表記（実際の開始時刻はDay 0当日に企画者が案内）。

| 経過 | 所要 | 内容 |
|---|---|---|
| 0〜5分 | 5分 | 挨拶と目的 |
| 5〜15分 | 10分 | 役割決め |
| 15〜40分 | 25分 | Git一周演習 |
| 40〜50分 | 10分 | Discord案内＋今後の進め方 |
| 50〜60分 | 5〜10分 | バッファ |

合計55〜60分。45分しかない日は「バッファ」と「役割決めの深掘り」を削って対応する（詳細は「5. 時間切れ時の持ち越しルール」）。

### 0〜5分：挨拶と目的

- このチームで作るもの（教室の温度ムラ見える化）を一言で確認。
- 進め方の基本ルールを短く確認：
  - AIルール4つ（共通テンプレート／データ契約を守る／説明できないコードはマージしない／AI利用箇所をREADMEに明記）
  - Gitはfeatureブランチ→PR→企画者レビュー→マージ。mainへの直接pushはしない。
  - 公開リポジトリなので、学校名や教室が特定される写真・情報は書かない。

### 5〜15分：役割決め

- 候補：ノード担当（全員1人1台）／集約表示オーナー（1名＋企画者伴走）／データ分析／発表とりまとめ／筐体（拡張・余力があれば）。
- 全員がノード担当を兼務しつつ、上記の中から得意・興味のある役割を1つ以上表明する。
- 時間内に決めきれなければ「仮決め」でよい（「5. 時間切れ時の持ち越しルール」参照）。

### 15〜40分：Git一周演習（25分）

3章参照。全員がミニPRを1本、最後まで通す。

### 40〜50分：Discord＋今後の進め方

- Discordサーバーへの参加を確認（宿題でアカウントは作成済みのはず）。
- チャンネル構成・連絡ルールを簡単に紹介。
- 次回（8/21中間デモ）までに各自やることを一言ずつ確認。

### 50〜60分：バッファ

- 上記が押した分の吸収、質疑応答。

## 3. Git一周演習の手順

目的：`day0/<自分のハンドル>.md` を新規作成して自己紹介を1行書き、ブランチ作成→commit→push→PR作成→企画者レビュー→マージ、を全員が1周する。

`<handle>` は各自のGitHubユーザー名など、他の人と被らない短い文字列に置き換える。

```bash
# 1. リポジトリの最新化（ローカルにclone済みの前提）
git checkout main
git pull

# 2. 自分用のブランチを作る
git checkout -b day0/<handle>

# 3. ファイルを作る（VSCodeで day0/<handle>.md を新規作成し、自己紹介を1行書いて保存）
#    例: 「n1担当の◯◯です。よろしくお願いします。」

# 4. 変更をステージしてコミット
git add day0/<handle>.md
git commit -m "day0: <handle>の自己紹介を追加"

# 5. リモートにpush
git push -u origin day0/<handle>
```

続けてブラウザ側の操作：

1. push後にターミナルへ出るURL（またはGitHub上のリポジトリページ）を開き、「Compare & pull request」からPRを作成する。
2. PRの説明欄（`.github/pull_request_template.md` のテンプレート）に沿って、やったことを一言書く。
3. 企画者がレビューして、問題なければ「Approve」→マージボタンでマージする。
4. マージされたらDay 0の演習は完了。

つまずきやすい点（企画者が机間巡回で見るポイント）：

- `git checkout -b` を忘れてmainのまま作業してしまう → ブランチ名を都度声かけで確認。
- `git push` 時にリモートが無い／認証エラー → 事前準備（0章）でclone・認証が済んでいるか確認。
- PRのターゲットブランチが `main` になっているか確認。

## 4. Day 0完了条件チェックリスト

- [ ] 全員の `day0/<handle>.md` PRがマージ済み
- [ ] 全員がDiscordサーバーに参加済み
- [ ] 役割が（仮決めでも）確定している

## 5. 時間切れ時の持ち越しルール

45分しかない、または押してしまい60分でも終わらない場合の優先順位。

1. 最優先（当日中に必ず終える）：Git一周演習を全員が最後（マージ）まで終える。
2. 次点：役割は仮決めでよいので全員が希望だけ表明する（正式決定は次回放課後に持ち越し）。
3. 削ってよい：Discordの今後の進め方説明の詳細部分、雑談的なやり取り。要点だけDiscordのテキストで後日共有する。
4. Git演習が時間内に終わらなかったメンバーが個別にいる場合：その人だけ翌回の放課後に個別フォローする。全体の進行は止めない。

【要確認】「翌回の放課後」が具体的にいつ確保できるか（訓練校の時間割・8/17以降の曜日ごとの空き）は未確認。企画者がDay 0終了時にDiscordで次回日時を案内する運用とする。
