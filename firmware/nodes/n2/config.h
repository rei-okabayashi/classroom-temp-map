#pragma once
#include <stdint.h>

// ================================================================
// ここだけ自分用に書き換える（他は変えない）
// ================================================================

// 自分のノードID（"n1"〜"n4"）。設置場所との対応表はREADME参照
#define NODE_ID "n2"

// 集約M5StackのMACアドレス。
// M5Stackを起動すると画面とシリアルに "MAC: 24:0A:C4:xx:xx:xx" と
// 表示されるので、その値を1バイトずつ 0x?? 形式で転記する
static const uint8_t GATEWAY_MAC[6] = {0x00, 0x00, 0x00, 0x00, 0x00, 0x00};

// ================================================================
// チームで固定している値（データ契約v1。変えるときは合意してv+1）
// ================================================================

#define SCHEMA_VERSION   1
#define WIFI_CHANNEL     1        // ESP-NOWチャンネル。送受で一致必須
#define SEND_INTERVAL_MS 30000UL  // 送信周期30秒

// SHT31のI2Cアドレス。
// 秋月 AE-SHT31 はデフォルト 0x45（基板上でADRがプルアップ）。
// ADRパッドをGNDに半田ジャンパすると 0x44 になる
#define SHT31_ADDR 0x45

// I2Cピン（ESP32-DevKitC 標準）。配線: 3V3, GND, SDA=21, SCL=22
#define PIN_SDA 21
#define PIN_SCL 22
